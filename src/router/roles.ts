// import { Router } from 'express';
// import { getRoles } from '../controllers/roles.controller';
// import { checkToken } from '../middlewares/authentication.middlewares';
// import { checkAccess } from '../middlewares/authorization.middleware';

// const router = Router();

// router.get('/roles', [checkToken, checkAccess], getRoles);

// export default router;
